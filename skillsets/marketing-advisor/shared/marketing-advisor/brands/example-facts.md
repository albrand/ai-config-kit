# Fact sheet (the only allowed source of facts)

Replace with what the product actually is and does, what it must never promise, and any identification rules (for example, show the CRP whenever a psychologist is named).
