# Library and directive audit — 2026-08-20

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none. `delegating-to-glm` and `measuring-agent-spend` still overlap on subprocess attribution, but that consolidation is already tracked/applied; filing it again would duplicate the experiment.

RETIRE: `CLAUDE.md — Token economy` should drop the requirement to read `TOKEN_ECONOMY.md` “on every sub-agent call.” The useful defaults—smallest tier, bounded brief, output filtering, progressive disclosure—already appear inline. Unconditionally loading another document expands the dominant cached-input cost, especially for Claude’s measured 3.4M weighted tokens/turn.

CONFLICT: `CLAUDE.md — Sub-agent lifecycle freshness` says “open a fresh agent rather than reuse stale context,” while `CLAUDE.md — Thread housekeeping` says “prefer continuing an existing thread… review rounds especially must share context.” This needs the already-open same-review exception, not another optimization. `CLAUDE.md — Browser surface isolation` says Orca is “the only permitted interactive browser surface,” while the bb `isolated-browser` lesson requires bb’s isolated browser for real-browser work. The global directive hard-codes a machine-specific transport instead of deferring to the active harness’s approved isolated surface.

GAP: none. High `git`, `grep`, and `bb` counts demonstrate frequent repository and orchestration work, but tool-label attribution does not establish a missing reusable procedure; existing directives already cover inspection, validation, and bb routing.

SHARPEN: none. The obvious candidate, `orchestrator-lessons`, has already had its broad trigger narrowed; proposing that again would duplicate an applied experiment.

OPTIMIZATION | Remove CLAUDE.md Token economy’s unconditional TOKEN_ECONOMY.md read on every sub-agent call; retain the inline bounded-brief defaults and load detailed guidance only for cost/context work | avoids adding a full policy document to delegation turns where cached input already dominates spend  
OPTIMIZATION | Replace CLAUDE.md Browser surface isolation’s Orca-only mandate with a short requirement to use the active harness’s approved isolated-browser skill | removes duplicated machine-specific policy and prevents costly blocked/retried browser workflows in bb
