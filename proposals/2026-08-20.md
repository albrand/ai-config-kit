# Library and directive audit — 2026-08-20

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none. `token-economics` interprets cost/context behavior; `measuring-agent-spend` implements accounting and visualization. Their remaining overlap does not justify combining distinct triggers.

RETIRE: `AGENTS.md — Core operating principles` item 5’s default sibling-project scan is dead as an always-on rule. It says “Proactively — as a default planning step… scan sibling projects,” although delegated packets already supply bounded scope and the measured workload is dominated by 20,788 no-tool turns and cached input. Repository comparison should be requested by an architecture task, not imposed on every non-trivial plan.

CONFLICT: `AGENTS.md — Delegated fast path` says “Do not… scan sibling projects… or load unlisted skills,” while `AGENTS.md — Core operating principles` item 5 says “ALWAYS run an independent debiasing critique” and “as a default planning step… scan sibling projects.” A complete delegated architecture packet triggers both. The fast path requires following the supplied plan without rediscovery; item 5 mandates rediscovery and another model. The earlier fast-path planning reconciliation did not resolve this counterpart/sibling-scan conflict.

GAP: none. `git`, `grep`, `bb`, and `python3` frequency identifies tools, not an uncovered lesson. Subprocess invisibility also prevents a defensible provider-routing gap claim.

SHARPEN: none. The only conspicuously broad description, `orchestrator-lessons`, already has an applied narrowing experiment despite the stale inventory text.

OPTIMIZATION | Exempt AGENTS.md delegated and advisor packets from Core operating principles item 5, and remove its default sibling-project scan while retaining counterpart critique for master-thread architecture decisions | avoids redundant repository discovery and counterpart context on bounded OpenCode runs, whose cached-input cost averages 3.7M weighted tokens per turn
