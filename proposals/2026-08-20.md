# Library and directive audit — 2026-08-20

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none.

RETIRE: `AGENTS.md — Workflow tracks, breakpoints, quality convergence` should leave the always-on file. Its procedures are already routed to the on-demand `big-change-planning` and `quality-convergence` skills by `AGENTS.md — Skills (on-demand, progressively loaded)`. Carrying the expanded definitions on all 306 OpenCode turns contradicts the stated trigger-layer design and contributes to OpenCode’s measured 3.7M weighted tokens/turn.

CONFLICT: `AGENTS.md — Core operating principles` says “Delegate when useful,” while `AGENTS.md — cmux + Hermes Control Plane` says “Delegation defaults off with concurrency/depth 1.” “Defaults off” suppresses the proportional delegation the earlier rule authorizes; concurrency/depth limits can remain without disabling delegation. Separately, `AGENTS.md` says “This file is the always-on trigger layer… Keep this file lean,” but `AGENTS.md — Workflow tracks, breakpoints, quality convergence` embeds detailed procedures already assigned to on-demand skills.

GAP: none. Frequent `git`, `grep`, `bb`, and `python3` labels establish common tools, not a missing reusable workflow. `bb-plugin-authoring`, `bb-sdk-traps`, and the standing inspection/validation directives already cover the supported categories.

SHARPEN: none. `orchestrator-lessons` remains broadly described in the supplied inventory, but narrowing it is already applied and must not be re-filed.

OPTIMIZATION | Remove AGENTS.md’s “Delegation defaults off” clause while retaining concurrency/depth, one-writer, and transport-security limits | prevents unnecessary master-thread work caused by an internal routing contradiction and removes a small always-loaded clause  
OPTIMIZATION | Reduce AGENTS.md Workflow tracks, breakpoints, quality convergence to on-demand skill triggers | removes duplicated procedure text from every OpenCode turn, where cached input averages 3.7M weighted tokens per turn
