# Library and directive audit — 2026-08-17

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: `delegating-to-glm` and `measuring-agent-spend` duplicate the subprocess-attribution trap. Make `measuring-agent-spend` canonical for interpreting event/provider totals; keep `delegating-to-glm` focused on choosing accountable thread invocation versus unaccounted subprocess execution.

RETIRE: `CLAUDE.md — Directive challenge` duplicates `CLAUDE.md — Default process`, item 7: both cover non-trivial planning/framework/skill critique, counterpart use, challengeable prior evidence, precedence, and the identical authorization sentence. The second always-on block is dead duplication. Do not retire `CLAUDE.md — opencode delegation` from only 98 recorded OpenCode turns; `measuring-agent-spend` and the stated subprocess blind spot invalidate that inference.

CONFLICT: `CLAUDE.md — One delegation rule` says “nothing elsewhere overrides this” and “no obligation to delegate when it adds nothing.” `CLAUDE.md — Multi-agent orchestration` nevertheless requires “At intake find ≥1 bounded sidecar,” while `CLAUDE.md — opencode delegation` mandates “you delegate EXECUTION to opencode.” Remove both unconditional mandates; retain proportional routing.

GAP: none. `git`, `grep`, `bb`, and `python3` are repeatedly used, but `CLAUDE.md — Default process`, `AGENTS.md — Core operating principles`, and `AGENTS.md — Preserve context economy` already cover repository inspection, orchestration, and deterministic preprocessing. Turn-level attribution cannot justify another lesson.

SHARPEN: `orchestrator-lessons` grew from 181 to 222 lines while retaining “running a fleet” and “same class of mistake” as its triggers. Those phrases omit likely matching language already implied by its purpose: fleet completion, close-out audit, repeated orchestration failure, and failed review. Add those terms and remove the catch-all “Read before declaring work done,” which overmatches routine work.

OPTIMIZATION | Remove `CLAUDE.md — Directive challenge`; retain `Default process` item 7 | Eliminates duplicate cached input on every Claude turn  
OPTIMIZATION | Delete mandatory intake-sidecar and mandatory opencode-execution wording in favor of `One delegation rule` | Avoids unnecessary high-cost delegated turns  
OPTIMIZATION | Centralize subprocess attribution in `measuring-agent-spend` and trim `delegating-to-glm` | Prevents duplicate skill context during spend investigations  
OPTIMIZATION | Narrow `orchestrator-lessons` to explicit fleet-failure and close-out triggers | Avoids loading 222 lines for routine work
