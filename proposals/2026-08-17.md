# Library and directive audit — 2026-08-17

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: `delegating-to-glm` and `measuring-agent-spend` duplicate the subprocess-accounting lesson. Keep invocation/accountability mechanics in `delegating-to-glm`; make `measuring-agent-spend` the single source for interpreting provider totals and subprocess blind spots.

RETIRE: `CLAUDE.md — Directive challenge` is a near-duplicate of `CLAUDE.md — Operating framework`, item 7, including counterpart critique, authorization text, sibling scanning, and precedence. Remove the duplicate block. Do **not** retire `CLAUDE.md — opencode delegation` merely because OpenCode records only 88 turns: `measuring-agent-spend` explicitly says subprocess runs are invisible.

CONFLICT: `CLAUDE.md — Multi-agent orchestration` says, “At intake find ≥1 bounded sidecar,” while `CLAUDE.md — One delegation rule` says there is “no obligation to delegate when it adds nothing.” `CLAUDE.md — opencode delegation` further says execution delegation is “mandatory.” Replace both mandates with the proportional rule; Claude’s 5.6M weighted tokens/turn and OpenCode’s 682k make unnecessary delegation expensive. Separately, `AGENTS.md — cmux + Hermes Control Plane` says “Delegation defaults off with concurrency/depth 1,” while `AGENTS.md — Running as a delegated executor` permits “up to 10 opencode instances.” Scope the former explicitly to cmux cross-session delegation.

GAP: none. `git`, `grep`, `bb`, and `python3` counts demonstrate recurring repository work, but the attribution is turn-level and existing directives already cover inspection, orchestration, and deterministic preprocessing.

SHARPEN: `orchestrator-lessons` is too broad: “running a fleet” plus “Read before declaring work done” can match nearly any delegated task. Name its concrete triggers—writer collisions, stale-agent reuse, integration drift, missing independent validation, and orphan cleanup—so its 181 lines load only for fleet close-out or repeated orchestration failures.

OPTIMIZATION | Remove `CLAUDE.md — Directive challenge`; retain `Operating framework` item 7 as the canonical rule | Cuts duplicated cached input on every Claude turn  
OPTIMIZATION | Replace mandatory intake sidecars and mandatory opencode execution with `One delegation rule` proportional routing | Avoids costly agent turns when delegation adds no value  
OPTIMIZATION | Consolidate subprocess-accounting guidance from `delegating-to-glm` into `measuring-agent-spend` | Prevents duplicate skill context on delegation-cost investigations  
OPTIMIZATION | Narrow the `orchestrator-lessons` description to concrete fleet failure and close-out triggers | Avoids loading 181 lines for ordinary delegated work
