# Library and directive audit — 2026-08-17

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: `delegating-to-glm` and `measuring-agent-spend` overlap on the subprocess-accounting blind spot. Move all attribution interpretation into `measuring-agent-spend`; retain only invocation and provider-accountability choices in `delegating-to-glm`.

RETIRE: `CLAUDE.md — Directive challenge` duplicates `CLAUDE.md — Default process`, item 7: both require independent critique, challenge prior directives, preserve precedence, and include the same authorization sentence. The separate always-on block is dead weight. Do not retire `CLAUDE.md — opencode delegation` from OpenCode’s 97 recorded turns; `measuring-agent-spend` says subprocess usage is absent from provider totals.

CONFLICT: `CLAUDE.md — Multi-agent orchestration` requires “At intake find ≥1 bounded sidecar,” and `CLAUDE.md — opencode delegation` says “you delegate EXECUTION to opencode,” while `CLAUDE.md — One delegation rule` says “no obligation to delegate when it adds nothing.” The proportional rule must control. `AGENTS.md — Delegated fast path` says “Do not…create a new plan/todo,” while `AGENTS.md — Running as a delegated executor` says delegated mode must “still apply analysis, planning.” Clarify that executors analyze and follow the supplied plan but never re-plan.

GAP: none. Frequent `git`, `grep`, `bb`, and `python3` usage is already covered by inspection, orchestration, and deterministic-preprocessing directives; turn-level attribution cannot establish a narrower missing lesson.

SHARPEN: `orchestrator-lessons` matches nearly everything through “running a fleet” and “Read before declaring work done.” Replace that with concrete triggers: multi-writer collisions, stale-agent reuse, integration drift, orphan cleanup, repeated delegated failures, or fleet close-out. Its 181 lines should not load for routine single delegation.

OPTIMIZATION | Remove `CLAUDE.md — Directive challenge` and retain `Default process` item 7 | Eliminates duplicated cached input from every Claude turn  
OPTIMIZATION | Make `One delegation rule` override mandatory intake-sidecar and opencode-execution wording | Avoids unnecessary high-cost agent turns  
OPTIMIZATION | Reconcile `AGENTS.md — Delegated fast path` with delegated-executor planning as follow-plan-without-replanning | Prevents redundant planning tokens in delegated runs  
OPTIMIZATION | Narrow the `orchestrator-lessons` trigger to concrete fleet failures and close-out | Avoids loading 181 lines during routine delegation  
OPTIMIZATION | Centralize subprocess attribution in `measuring-agent-spend` and trim `delegating-to-glm` | Removes duplicated skill context during spend investigations
