# Library and directive audit — 2026-08-23

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none

RETIRE: Retire the Codex-to-OpenCode translation note from `AGENTS.md — Skills (on-demand, progressively loaded)` and place it in `skill-library-router`. It applies only after loading a skill containing Codex-specific model or concurrency instructions; carrying examples such as “GPT 5.3 Spark,” `max_concurrent_threads_per_session`, and `claude -p` on unrelated turns is dead standing context.

CONFLICT: none

GAP: none

SHARPEN: none

OPTIMIZATION | Move the Codex-specific skill translation note from AGENTS.md into skill-library-router | Removes conditional compatibility guidance from every OpenCode turn and loads it only when a mismatched skill actually needs translation
