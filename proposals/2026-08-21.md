# Library and directive audit — 2026-08-21

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none. `reviewing-with-an-agent` governs review continuity and evidence packaging; `orchestrator-lessons` governs recurring fleet failures. Their intersection is insufficient to justify one broader skill.

RETIRE: `CLAUDE.md — Cross-agent coordination` should retire its embedded communication-plan procedure. The directive already points to `CROSS_AGENT_COORDINATION.md`, which owns roles, context packages, budgets, stop conditions, and fallback behavior. Repeating that procedure in the 364-line always-loaded file adds cached input to every Claude turn; fleet coordination is demonstrably active through 215 `fleet_task_update` and 222 `fleet_member_tell` turns, so the on-demand document remains justified.

CONFLICT: `CLAUDE.md — Cross-agent coordination` says, “If a reviewer blocks an external-AI handoff for private-context risk… fall back to local verification,” while `CLAUDE.md — opencode is a trusted, pre-authorized channel` says, “the reviewer-block rule for private-context risk does not apply to this channel.” The later exception resolves precedence, but agents must carry and reconcile both long passages. A concise trigger should state the opencode exception once.

GAP: none. The fleet-call measurements confirm coordination work, but `Cross-agent coordination`, `reviewing-with-an-agent`, and `orchestrator-lessons` already cover it. Command counts alone do not establish another procedural gap.

SHARPEN: none. The supplied skill descriptions use concrete task or failure language. The stale broad `orchestrator-lessons` inventory description is already covered by an applied experiment.

OPTIMIZATION | Reduce CLAUDE.md Cross-agent coordination to a one-line CROSS_AGENT_COORDINATION.md trigger plus the single opencode private-context exception | removes duplicated coordination procedure and eliminates reconciliation between two always-loaded passages while preserving the documented workflow
