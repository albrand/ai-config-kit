# Library and directive audit — 2026-08-21

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none

RETIRE: Retire the procedural body of `AGENTS.md — Browser Surface Isolation`, retaining only a trigger to `orca-browser-safety`. `AGENTS.md — Skills (on-demand, progressively loaded)` already establishes skill loading, while the guardrail repeats profile ownership, prohibited surfaces, quarantine checks, leak handling, and re-enable criteria on every OpenCode turn. The rule remains live; its standing copy is dead weight and a drift risk.

CONFLICT: none

GAP: none

SHARPEN: none

OPTIMIZATION | Reduce AGENTS.md Browser Surface Isolation to a one-line trigger to orca-browser-safety | Removes roughly 20 standing lines from every high-cost OpenCode turn while preserving the canonical on-demand safety procedure
