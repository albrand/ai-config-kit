# Library and directive audit — 2026-09-12

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

## MERGE
none beyond the pending consolidation proposals.

## RETIRE
none newly supported. **delegating-to-glm** cannot be retired on provider counts: subprocess execution is explicitly missing from that accounting. The historical material in **CLAUDE.md — Worktree lifecycle** and **No feature flags without an explicit ask** already has an open removal proposal.

## CONFLICT
none newly actionable beyond the pending conflict-resolution proposals.

## GAP
none. For **measuring-agent-spend** and **token-economics**, the increased `cmd:bb`, `cmd:grep`, and `cmd:sleep` counts do not establish missing procedures or wasteful polling. Turn-level attribution lacks the command arguments, purpose, and outcomes needed to support those conclusions.

## SHARPEN
none newly evidenced. **orchestrator-lessons** already has pending trigger-narrowing proposals; its repeated description is not grounds to register another.
