# Library and directive audit — 2026-09-24

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none

RETIRE: none

CONFLICT: **qa-sweep — description versus P6 Ship.** The description says “before shipping (git push, gh pr create/merge, bb fleet validate, any deploy)” and “The gate denies the ship until the pipeline is complete.” P6 instead says “Free on purpose: **feature-branch pushes**,” “**`gh pr create`**,” and “**`bb fleet validate`**.” The description retains the broader gate boundary that P6 explicitly supersedes, potentially prompting premature sweeps or blocking the preview needed for validation.

GAP: none

SHARPEN: **qa-sweep** — replace the description’s blanket command list with P6’s actual gated operations: merges/ready, protected-destination pushes, and production deployments. Explicitly exempt feature-branch pushes, PR creation, and review submission; retain the workflow-defect trigger and all required QA stages.

OPTIMIZATION | Align qa-sweep’s description with P6 Ship: remove blanket push, PR-create, fleet-validate and any-deploy triggers; name the actual gated operations and preserve the workflow-defect trigger | Avoids premature pipeline loads and preview/review deadlock recovery without reducing pre-ship QA; savings remain to be measured
