# Library and directive audit — 2026-09-23

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none

RETIRE: none

CONFLICT: **CLAUDE.md — Multi-agent orchestration** prescribes `Agent(model: "haiku")` and `Agent(model: "sonnet")`. **Coordinate only when it pays** instead sends implementation to “durable bb child threads … not harness-internal subagents”; **Global child-agent routing** requires spawning with “exactly the returned `providerId`, `model` and `reasoningLevel`.”

Remove the static agent/model prescriptions from the bb-applicable portion of **Multi-agent orchestration**, retaining its responsibility boundaries. Keep standalone Claude routing explicitly outside bb. This is distinct from the queued correction to **AGENTS.md — GLM-5.3 route**.

GAP: none

SHARPEN: none

OPTIMIZATION | Remove bb-applicable hardcoded Haiku/Sonnet Agent routing from CLAUDE.md — Multi-agent orchestration; defer to Coordinate only when it pays and Global child-agent routing, preserving standalone guidance and responsibility boundaries | Shortens competing routing instructions and could avoid wrong-surface dispatches and corrective handoffs; savings remain an experiment, not an observed result
