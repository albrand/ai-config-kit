# Library and directive audit — 2026-09-11

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

## MERGE
none

## RETIRE
**CLAUDE.md — Worktree lifecycle** and **No feature flags without an explicit ask**: retire the historical inventories and incident narration, not the safeguards.

The worktree count, sampled dependency sizes, existing-flag reference counts, and PR #847 anecdote describe particular snapshots; they cannot establish current state. Carrying them every turn adds context without changing the required decision.

Retain ownership, dirty-tree and detached-commit protections, `.keep-worktree`, cleanup obligations, explicit flag authorization, existing-flag exclusions, removal plans, and regression assertions. Remove the numerical inventories and historical justification surrounding those rules.

## CONFLICT
none beyond conflicts already covered by pending proposals.

## GAP
none. **measuring-agent-spend** is the relevant attribution lesson: the supplied `cmd:git`, `cmd:grep`, and no-tool-call aggregates do not identify an uncovered workflow. Subprocess invisibility also prevents interpreting provider absence as non-use.

## SHARPEN
none supported by the supplied descriptions alone.

OPTIMIZATION | Remove historical worktree inventories, flag reference counts, and incident narration from CLAUDE.md’s Worktree lifecycle and No feature flags without an explicit ask; preserve their operative safeguards and regression requirements | Reduces always-loaded input on Claude turns without adding skill loads or weakening decision rules; evaluate input-token reduction separately from changes in workload mix
