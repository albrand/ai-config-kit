# Library and directive audit — 2026-08-18

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none.

RETIRE: none. `CLAUDE.md — Ecosystem terraform commands` and `CLAUDE.md — PR review command` remain oversized, but their reduction is already OPEN and must not be re-filed. OpenCode’s 98 recorded turns do not retire any OpenCode directive because `measuring-agent-spend` documents the subprocess blind spot.

CONFLICT: `CLAUDE.md — Default process`, item 7, says non-trivial planning, architecture, framework, or skill decisions must “ALWAYS run an independent debiasing critique through a second model/counterpart.” `CLAUDE.md — One delegation rule` says there is “no obligation to delegate when it adds nothing.” The unconditional counterpart mandate overrides proportional routing across a very broad task class. Make counterpart critique risk-based—architecture, security, data, auth, release, or unresolved high-impact ambiguity—and permit local self-critique otherwise.

GAP: none. Rising `fleet_task_update`, `fleet_member_tell`, and `fleet_context_set` activity is covered by `fleet-orchestration`; `orchestrator-lessons` covers repeated fleet failures and completion challenge. `sleep` and `uptime` remain too ambiguous at turn-level attribution to justify a new lesson.

SHARPEN: none. The broad `orchestrator-lessons` description shown in the library conflicts with the previously verified narrowed state, but that optimization is already APPLIED and should be reconciled as stale inventory rather than proposed again.

OPTIMIZATION | Make `CLAUDE.md — Default process` item 7 counterpart critique risk-based and subordinate to `One delegation rule` | Avoids an extra high-cost agent turn for ordinary planning, framework, and skill decisions
