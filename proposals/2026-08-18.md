# Library and directive audit — 2026-08-18

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none.

RETIRE: retire the operational implementation details in `CLAUDE.md — Thread housekeeping` from standing context. The 15-minute scan, 6h/24h thresholds, archive semantics, `bb fleet orphans` syntax, and active-orphan gap are relevant only during fleet administration and are already owned by `fleet-orchestration` plus the automated `fleet-housekeeping` service. Keep one invariant: attach spawned threads to a group and continue the same thread for review rounds. With 99% cache share, carrying the service manual through every Claude turn is dead weight.

CONFLICT: none. The remaining conflict between `CLAUDE.md — Default process` item 7 (“ALWAYS run an independent debiasing critique”) and `CLAUDE.md — One delegation rule` (“no obligation to delegate when it adds nothing”) already has an OPEN optimization and should not be re-filed.

GAP: none. Frequent `fleet_task_update` and `fleet_member_tell` turns are covered by `fleet-orchestration`; command counts for `grep`, `git`, `sed`, `sleep`, and `uptime` show activity, not an uncovered recurring failure.

SHARPEN: none. The displayed broad `orchestrator-lessons` description is stale inventory relative to its APPLIED narrowing and does not justify another proposal.

OPTIMIZATION | Reduce `CLAUDE.md — Thread housekeeping` to its group-membership and same-review invariants; move service thresholds, CLI syntax, and orphan mechanics to `fleet-orchestration` | Removes fleet-only operational detail from every cached Claude turn
