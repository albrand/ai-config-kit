# Library and directive audit — 2026-08-22

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none

RETIRE: Retire `AGENTS.md — Routing tiers` bullet `Local sidecar candidates`. It describes MLX/Qwen and `ollama-4090` as capabilities available only “once defined as routed agents,” so it is not an executable routing rule. Provider configuration is mutable runtime state and should be discovered from `opencode.jsonc`, not cached in every turn. Keep the invariant that sidecar output is not source-of-truth in the eventual routed-agent definition.

CONFLICT: none

GAP: none

SHARPEN: none

OPTIMIZATION | Remove the speculative Local sidecar candidates bullet from AGENTS.md Routing tiers | Eliminates dormant configuration-specific text from every OpenCode turn until those agents actually exist and need an on-demand definition
