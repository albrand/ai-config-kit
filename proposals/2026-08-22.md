# Library and directive audit — 2026-08-22

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none

RETIRE: Retire `AGENTS.md — Always report changes` as a separate always-on directive. `AGENTS.md — Validation truthfulness and completion standard` already requires implementation close-out to state “what changed.” Requiring another report after every mutating command is redundant and can repeatedly expand delegated context; retain one changed-path summary in the close-out contract.

CONFLICT: `AGENTS.md — Delegated fast path` says, “At close-out, emit only the compact contract.” `AGENTS.md — Always report changes` says, “After making any change … report it in the same turn” and “List every file/path touched.” The latter can force intermediate or extra output beyond the executor’s bounded contract.

GAP: none

SHARPEN: none

OPTIMIZATION | Delete AGENTS.md Always report changes and require touched paths only in the existing implementation close-out contract | Removes duplicated standing instructions and prevents mutation-by-mutation context growth during delegated runs
