# Library and directive audit — 2026-08-22

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none

RETIRE: Retire the hard-coded “Notable skills” enumeration from `AGENTS.md — Skills (on-demand, progressively loaded)`. The same directive says the library is dynamically wired through `skills.paths` and skills are selected by `name`/`description`. A static names-only list adds no trigger descriptions, duplicates discoverable metadata, and becomes stale whenever the library changes. Retain the dynamic matching rule and `skill-library-router`.

CONFLICT: none

GAP: none

SHARPEN: none

OPTIMIZATION | Remove the hard-coded Notable skills list from AGENTS.md and rely on skills.paths metadata plus skill-library-router | Deletes static standing tokens from every OpenCode turn and avoids repeatedly carrying a stale partial index
