# Library and directive audit — 2026-09-21

Proposed by Hermes, running on its own machine so the review is independent
and costs the local providers nothing. **Nothing here has been applied.**
Read it, argue with it, then edit the lessons yourself.

MERGE: none.

RETIRE: none. **AGENTS.md — GLM-5.3 route**’s claim that full routes are unhealthy is not established by provider turn counts; those measurements cannot justify retiring GLM or its delegation guidance.

CONFLICT: **AGENTS.md — GLM-5.3 route** says: “When GLM-5.3 is requested, use `zai-coding-plan/glm-5.3-flash`.” **Global child-agent routing (subscription-aware)** says: “spawn with exactly the returned `providerId`, `model` and `reasoningLevel`.”

For a bb child dispatch returning full GLM-5.3, these prescribe different models. Remove the static Flash substitution from bb child routing and defer to `bb fleet route`. Keep any standalone OpenCode fallback guidance outside the always-loaded directives, in **delegating-to-glm**, explicitly scoped outside bb. This does not assume either endpoint is currently healthy.

GAP: none. The supplied tool frequencies do not establish missing coverage in **verified-qa-e2e**, **verifying-rendered-ui**, or **measuring-agent-spend**.

SHARPEN: none beyond already-proposed changes.

OPTIMIZATION | Remove AGENTS.md GLM-5.3 route’s static Flash substitution for bb children; use Global child-agent routing’s exact fleet result and move standalone fallback guidance into delegating-to-glm | Shrinks the standing prefix and avoids conflicting model selection, redundant health probes, and rerouting
